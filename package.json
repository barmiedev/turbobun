{
  "name": "turbobun",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.3",
    "@repo/biome": "workspace:*",
    "@repo/typescript": "workspace:*",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "bun@1.0.30",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
